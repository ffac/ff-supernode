---

- hosts: "supernodes:{{ experimental_supernodes | default('') }}"
  pre_tasks:
    - name: "Import pre_tasks: reboot-required.yml"
      import_tasks: "include/reboot-required.yml"
      tags: [always]
    - name: "Import pre_task etckeeper"
      import_tasks: "include/etckeeper_pre.yml"
      tags: [always]
  roles:
    # General
    - { role: ff.mesh_batman, tags: mesh_batman, become: true }
    - { role: ff.ipforward, tags: ipforward, become: true }
    - { role: ff.networkd, tags: networkd, become: true }
    - { role: ff.backbone, tags: backbone, become: true }
    - { role: ff.mesh_wireguard, tags: mesh_wireguard, become: true }
    - { role: ff.mesh_wireguard_remotes_peers_git, tags: mesh_wireguard_remotes_peers_git, become: true }
    - { role: ff.radv_server, tags: radv_server, become: true }
    - { role: ff.kea_dhcp4_server, tags: dhcp, become: true }
    - { role: ff.iptables, tags: iptables, become: true }
    - { role: ff.mesh_announce, tags: mesh_announce, become: true }
    - { role: ff.watchdog, tags: watchdog, become: true }
    - { role: ff.yanic, tags: yanic, become: true }
    - { role: ff.supernode_scripts, tags: supernode_scripts}
    #- { role: ff.gw-statuspage, tags: gw_statuspage, become: true }
    # Backups
    #- { role: ff.restic, tags: restic }
  post_tasks:
    - name: "Import post_task etckeeper"
      import_tasks: "include/etckeeper_post.yml"
      tags: [always]
