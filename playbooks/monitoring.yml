---

- hosts: "monitoring"
  pre_tasks:
    - name: "Import pre_tasks: reboot-required.yml"
      import_tasks: "include/reboot-required.yml"
      tags: [always]
    - name: "Import pre_task etckeeper"
      import_tasks: "include/etckeeper_pre.yml"
      tags: [always]
  roles:
    # General
    - { role: ff.influxdb, tags: influxdb, become: true }
    - { role: ff.yanic, tags: yanic }
    - { role: ff.grafana, tags: grafana, become: true }
    - { role: ff.certbot, tags: certbot, become: true }
    - { role: ff.nginx, tags: nginx, become: true }
    - { role: ff.meshviewer, tags: meshviewer, become: true }
    - { role: ff.wgbroker, tags: wgbroker, become: true }
  post_tasks:
    - name: "Import post_task etckeeper"
      import_tasks: "include/etckeeper_post.yml"
      tags: [always]
